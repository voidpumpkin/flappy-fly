import { expect } from 'chai';
// import { FlappyFlyGame } from '../FlappyFlyGame.class';
// const { expect } = require('chai');
// const { FlappyFlyGame } = require('../FlappyFlyGame.class');

// describe('FlappyFlyGame', () => {
//     it('should be a Singleton', () => {
//         const flappyFlyGame1 = FlappyFlyGame.getInstance(document.body);
//         const flappyFlyGame2 = FlappyFlyGame.getInstance(document.body);
//         expect(flappyFlyGame1).to.eql(flappyFlyGame2);
//     });
// });

describe('index.js', () => {
    it('dummy test', () => {
        expect(true).to.eql(true);
    });
});
